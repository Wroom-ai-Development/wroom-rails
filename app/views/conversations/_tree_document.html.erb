
<%= button_to toggle_context_conversation_path(id: conversation.id, document_id: document.id), method: :patch, onclick: "refreshFrame(#{document.id});" do %>
  <% linked = conversation.documents.include?(document) %>
  <div class='tree-document <%= linked ? 'linked' : '' %>' id='tree-document-<%= document.id %>'>
    <span class='icon'>📄</span><%= document.title %>
  </div>
<% end %>
