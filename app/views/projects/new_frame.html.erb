  <%= turbo_frame_tag "new_project_frame" do %>
    <%= form_with(
      model: @project,
      url: create_from_frame_projects_url,
      id: 'new-project'
    ) do |form| %>

      <%= form.text_field :title, { placeholder: 'New project name' } %>
      <%= form.text_field :user_id, value: current_user.id, style: 'display: none;', readonly: cannot?(:manage, User) %>
      <%= form.submit '✙', style: 'font-style: normal;' %>

    <% end %>
  <% end %>
