<div id="record-form-view">
  <div id="record-form-box">
    <div id="header">
      <h3>Rename folder</h3>
      <%= link_to "❌", explorer_folder_path(id: @folder) %>
    </div>

    <%= render "form", folder: @folder, parent_id: @folder.parent_id %>

  </div>
</div>
